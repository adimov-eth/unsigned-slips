{
  "name": "p2p-slip-verifier",
  "version": "1.0.0",
  "description": "Working P2P bank-slip verifier. Proves an unsigned slip can be forged undetectably.",
  "scripts": {
    "extract": "bun run src/cli/extract.ts",
    "compare": "bun run src/cli/compare.ts",
    "verify": "bun run src/cli/verify.ts",
    "test": "bun test"
  },
  "keywords": ["pdf", "verification"],
  "license": "MIT",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  }
}
